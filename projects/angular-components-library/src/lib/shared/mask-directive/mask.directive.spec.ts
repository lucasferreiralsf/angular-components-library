import { SbaMaskDirective } from "./mask.directive";

describe("SbaMaskDirective", () => {
  it("should create an instance", () => {
    const directive = new SbaMaskDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
